/**
 * Remotion Index
 * 
 * Registers all compositions for the FlowAI Video Editor.
 * Each composition corresponds to a video project structure.
 */

import { registerRoot } from 'remotion';
import { RemotionRoot } from './Root';

registerRoot(RemotionRoot);
